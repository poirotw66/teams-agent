我看完你現在的 `arch/0919-convergence` 分支了。這一版已經做得差不多，**我建議不要再開第二輪大架構重構**。

目前 PR #7 已經是 **13 commits ahead of main**。上一輪 CI 是 `1762 passed / 5 failed`，而你剛剛最新的 `bb6e9ac` 已經專門修那 5 個 CI 問題，新一輪 CI 正在跑。

### 我認為你現在的下一步順序是

**1. 先把 PR #7 收乾淨、merge。**

目前你已經完成了：

- `main` branch protection ✅
- GCS Immutable Knowledge Release 成為 SoT ✅
- Knowledge image CI guardrail ✅
- Agent / Backoffice runtime dependencies 瘦身 ✅
- uv workspace 基礎 ✅
- `retrieval_query` / display trust boundary ✅
- Citation Asset Gateway 從 Teams Adapter 抽出 ✅
- Turn Planner PoC ✅
- OpenTelemetry foundation ✅

這已經達到「架構重整完成」的程度了。

**不要因為 `docs/uv-workspace-layout.md` 還寫著未來要搬成 `packages/` / `apps/`，就現在繼續搬。**

我會刻意停在這裡。

---

**2. 下一個真正該做的是 Golden User Journeys。**

這反而是我目前看到最大的缺口。

你的 repo 有非常多：

```text
unit tests
architecture tests
contract tests
Golden RAG Eval
release matrix
security tests
frontend tests
```

但是我搜尋不到一套真正明確叫做：

> 「使用者從入口走到出口，這條重要流程永遠不能壞」

的測試。

我會新增大約 **12 條**，不要貪多：

1. Teams 問知識問題 → RAG → Citation → Answer
2. FAQ → deterministic answer
3. 問題資訊不足 → clarification
4. 第二輪補資料 → 接續原 Issue
5. Multi-issue
6. IT + non-IT mixed message
7. Knowledge miss → no hallucination
8. Knowledge miss → Handoff
9. Handoff 後人工接手 → Agent 停止搶答
10. Feedback 👍 / 👎
11. Restricted document ACL
12. Conversation timeout / new conversation

測試最好打：

```text
API / Workflow boundary
↓
真實 workflow
↓
fake external services
↓
assert final observable behavior
```

而不是每個 class mock 一遍。

這套東西才是真正治你之前的：

> **「改 A 壞 B」**

---

### 3. Turn Planner 現在先別打開

你這次做法很好，放在：

```text
TURN_PLANNER_ENABLED=false
```

先保留。

而且你自己的最新 progress 文件已經發現一個很有趣的結果：

> Turn Planner 不一定真的比現有 Supervisor-first 少 LLM call。

因為你現在 Supervisor 對一些 standalone IT path 本來就有 optimization。

所以不要因為「Turn Planner 架構看起來比較漂亮」就換。

下一步應該真正跑：

```text
Current Workflow
vs
Turn Planner
```

比較三個東西：

| Metric | 要看什麼 |
|---|---|
| Accuracy | Golden Eval |
| Latency | P50 / P95 |
| Cost | LLM calls + token cost |

最後只有三種答案：

```text
Planner 明顯較好
→ enable

差不多
→ 留 current，刪 PoC

Planner 更差
→ 直接刪
```

**不要讓 feature flag PoC 永遠住在 production code 裡。**

這點很重要，否則半年後你的程式就會開始長滿「也許以後會用」的平行世界。

---

### 4. 然後立刻恢復功能開發

我覺得這才是現在最重要的轉折。

你前面停下來是因為：

> 「專案開始改不動了。」

所以現在不要問：

> 「架構還能不能再進步？」

改問：

> **「這次重構後，我新增一個真實功能，還會不會改 A 壞 B？」**

直接挑你 backlog 裡一個**中等複雜度功能**當驗收題。

不要挑 Hello World，也不要挑宇宙級功能。

例如：

```text
新功能
 ↓
需要碰 Agent workflow
 ↓
需要碰一點 Backoffice
 ↓
需要 UI
 ↓
需要測試
```

做完後記錄：

```text
修改多少 files？
跨多少 domains？
有沒有碰不該碰的 package？
CI 有沒有在 merge 前抓到 regression？
花多少時間在修 unrelated breakage？
```

這比再寫一份 30 頁 Architecture Review 更能證明這次重構有沒有成功。

---

## 我會新增一個指標：Change Radius

之後每個 feature PR 可以簡單記：

```text
Feature: XXX

Changed domains: 2
Changed production files: 7
Contracts changed: 1
Golden journeys affected: 2
Unrelated regressions: 0
```

不用做成企業級 KPI。

你只是想觀察：

### 重構前

```text
Feature A
→ 21 files
→ 6 domains
→ 修壞 3 個東西
```

### 重構後

```text
Feature B
→ 8 files
→ 2 domains
→ 0 unrelated regression
```

如果出現這種變化，就代表你這輪重構真的值了。

---

## OTel 可以做，但不是現在的主線

你現在只有：

```text
agent.workflow.run
```

這算 foundation，不是真正 distributed tracing。

以後可以慢慢長成：

```text
teams.inbound
    ↓
adapter.agent_request
    ↓
agent.workflow
    ├─ planner
    ├─ retrieval
    │   ├─ embedding
    │   └─ search
    ├─ llm.answer
    └─ persistence
```

但我會把這排在**功能開發旁邊慢慢補**。

不用停下整個產品再搞一次 observability 大工程。

---

# 所以現在我會給你一條很明確的路

```text
Architecture Convergence
        ↓
PR #7 CI Green
        ↓
Merge
        ↓
Golden User Journeys
        ↓
Turn Planner 真實 A/B
        ↓
決定 Keep / Delete / Enable
        ↓
══════════════════════
  大型架構重構結束
══════════════════════
        ↓
Feature Development
        ↓
每個 Feature 看 Change Radius
        ↓
遇到重複 3 次的架構痛點
才重新開 Architecture Issue
```

**其中我現在最推薦你下一個實作項目就是 Golden User Journeys。**

不是再拆 `packages/`，不是再拆 microservice，也不是把 Citation Asset Gateway 獨立部署。

因為你的問題從來不是「架構圖不夠漂亮」。

你的問題是：

> **「我想繼續開功能，但我怕碰一下旁邊就爆。」**

Golden User Journeys + 現在已經有的 branch protection / CI / domain boundary，才是把這個恐懼真正拔掉的最後一塊拼圖。

做完這塊，我會正式建議你**停止 Architecture Sprint，回產品開發**。