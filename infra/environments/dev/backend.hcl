# Template only: replace <dev-project-id> before a real backend initialization.
# This backend must not share a bucket or prefix with test, poc, or prod.
bucket = "<dev-project-id>-terraform-state"
prefix = "dev/teams-agent"
