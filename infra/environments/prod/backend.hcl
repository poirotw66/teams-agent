# Template only: replace <prod-project-id> before a real backend initialization.
# This backend must not share a bucket or prefix with dev, test, or poc.
bucket = "<prod-project-id>-terraform-state"
prefix = "prod/teams-agent"
